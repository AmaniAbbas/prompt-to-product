# Security Guidelines

- Never expose secrets in frontend code
- Protect private routes
- Validate user permissions on the server/backend
- Enforce role-based access
- Users must not access another user's private data
- File uploads must be access-controlled
- Sensitive operations require server-side checks
- Deletion/export flows must be deliberate and auditable